<p id="notice"><%= notice %></p>

<p>
  <strong>id:</strong>
  <%= @question.id %>
  <strong>内容:</strong>
  <%= @question.content %>
  <strong>ユーザーID:</strong>
  <%= @question.user_id %>
  <div>
    <strong>画像:</strong>
      <%= image_tag "/#{@question.image_name}" %>
  </div>
</p>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>

<p id="notice"><%= notice %></p>

<h1>Questions</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.content %></td>
        <td><%= link_to 'Show', question %></td>
        <td><%= link_to 'Edit', edit_question_path(question) %></td>
        <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%# <%= link_to 'New Question', new_question_path,class: 'btn btn-primary text-white' %>
<a href="/questions/new" class="btn btn-primary text-white">New Question</a>
<a href="/questions/new" class="btn btn-primary">New Question</a>
<a href="#" class="btn btn-primary">New Question</a>
